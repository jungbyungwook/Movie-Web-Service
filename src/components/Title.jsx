import styled from "styled-components";

export default function Title() {
    return <TitleWrapper>JMovie</TitleWrapper>;
}

const TitleWrapper = styled.h1`
    font-family: InterBold;
    display: inline;
    font-size: 35px;
    color: #ffffff;
`;
